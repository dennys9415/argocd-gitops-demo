apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base
  - hpa.yaml

patches:
  - path: patch-deployment.yaml

images:
  - name: nginx
    newTag: "1.25"

commonLabels:
  environment: staging